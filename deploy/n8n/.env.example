# Canonical n8n deployment settings.
# Copy to .env on the deployment host and set host-specific values there.

N8N_BIND_IP=127.0.0.1
N8N_WEBHOOK_URL=https://n8n.example.com/
N8N_HOST=n8n.example.com
N8N_PROTOCOL=https
N8N_SECURE_COOKIE=false

NTFY_HOSTNAME=ntfy.example.com
NTFY_HOST_IP=127.0.0.1
